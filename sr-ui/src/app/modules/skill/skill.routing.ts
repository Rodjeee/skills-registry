import { Routes } from '@angular/router';
import { SkillComponent } from './page/skill/skill.component';

export const SkillRoutingModule: Routes = [
    {
        path: 'skills-inventory',
        component: SkillComponent,
    }
]